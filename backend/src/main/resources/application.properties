spring.application.name=backend

# ==========================
# DATABASE
# ==========================
spring.datasource.url=jdbc:mysql://localhost:3306/ntc_staff?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================
# JPA / HIBERNATE
# ==========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate=WARN
logging.level.org.springframework.boot.autoconfigure.orm.jpa=INFO

# Recommended to disable the open-in-view warning
spring.jpa.open-in-view=false

# ==========================
# SERVER
# ==========================
server.port=8080

# ==========================
# MAIL (SMTP) ? replace with your credentials
# ==========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=edubishant@gmail.com
spring.mail.password=fjxmkjntljvarzxr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ==========================
# JWT ? use a strong secret (?32 chars)
# ==========================
jwt.secret=tJTILjN8cbiC9gN8gV2KQ9WQrxyqBaXk
# JWT expiration: 24 hours (in milliseconds)
jwt.expiration.ms=86400000

# ==========================
# OPTIONAL: Logging (for debugging)
# ==========================
logging.level.org.springframework.mail=DEBUG
logging.level.org.springframework.security=INFO
logging.level.com.ntc.backend.service.EmailService=DEBUG

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
file.upload-dir=uploads/signatures

